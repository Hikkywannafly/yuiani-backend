import { ISource, IEpisodeServer, IEpisode, IVideo, GetIdParams, IVideoServer } from './video-types';

export {
    ISource,
    IEpisodeServer,
    IEpisode,
    IVideo,
    GetIdParams,
    IVideoServer
}